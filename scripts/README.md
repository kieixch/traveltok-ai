# scripts/

Standalone scripts used across the monorepo (database seeding, data backfills,
dev utilities). Each script documents how to run it in its own header.

Scripts are executed from the repository root with `tsx`:

```bash
npx tsx scripts/example.ts
```
